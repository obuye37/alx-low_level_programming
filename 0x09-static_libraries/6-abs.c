#include "main.h"
#include <stdlib.h>
/**
 * _abs -  computes the absolute value of an integer.
 * @num: absolute value to compute
 * Return: always success
 */

int _abs(int num)
{
	int abs_num;

	abs_num = abs(num);
	return (abs_num);
}
